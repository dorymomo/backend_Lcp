<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	
	<!-- 일반회원 가입 -->

	<!-- 로그인 쿼리 -->
	<select id="login" parameterType="UserDTO" resultType="UserDTO">
		SELECT
		user_no,
		user_nickname,
		user_type
		FROM tbl_user
		WHERE user_id = #{userId}
		AND user_pw = #{userPw}
		AND user_status = 1
	</select>

	<!-- 아이디 중복체크 -->
	<select id="countByUserId" parameterType="string"
		resultType="int">
		SELECT COUNT(*)
		FROM TBL_USER
		WHERE USER_ID = #{userId}
	</select>

	<!-- 이메일 중복체크 -->
	<select id="countByUserEmail" parameterType="string"
		resultType="int">
		SELECT COUNT(*)
		FROM TBL_USER
		WHERE USER_EMAIL = #{userEmail}
	</select>

	<!-- 닉네임 중복체크 -->
	<select id="countByUserNickname" parameterType="string"
		resultType="int">
		SELECT COUNT(*)
		FROM TBL_USER
		WHERE USER_NICKNAME =
		#{userNickname}
	</select>

	<!-- 폰번호 중복체크 -->
	<select id="countByUserPhoneNum" parameterType="string"
		resultType="int">
		SELECT COUNT(*)
		FROM TBL_USER
		WHERE USER_PHONE_NUM = #{userPhoneNum}
	</select>
	
	
</mapper>